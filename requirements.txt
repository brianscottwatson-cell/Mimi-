# Core AI providers
anthropic>=0.39.0
openai>=1.0.0

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# HTTP client
httpx>=0.27.0
requests>=2.31.0

# Data / parsing
python-dotenv>=1.0.0
pydantic>=2.0.0
beautifulsoup4>=4.12.0
aiofiles>=23.0.0

# Twilio
twilio>=9.0.0

# Vector / RAG (optional — used when PINECONE_API_KEY is set)
pinecone-client>=3.0.0

# LiveKit (optional — used when LIVEKIT_API_KEY is set)
livekit>=0.13.0

# LangChain / LangGraph (optional advanced orchestration)
langchain>=0.2.0
langchain-anthropic>=0.1.0
langgraph>=0.1.0

# Utilities
websockets>=12.0
